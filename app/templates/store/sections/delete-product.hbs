<div id="deleteProductModal" class="ui tiny modal" style="color: black;">
    <div class="header">{{@model.product.name}}</div>
    <div class="content">Do you want to delete this product ?</div>
    <div class="actions">
        <button class="ui blue deny button">No</button>
        <button class="ui red deny button" {{action "delete" @model.product}}>Yes</button>
    </div>
</div>