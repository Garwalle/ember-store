<h1>Dashboard</h1>

<h3>Hello {{@model.connected.firstname}}, welcome to your dashboard !</h3>

<p>Your orders :</p>
<ul>
    {{#each @model.orders as |order|}}
    <li>{{order.dateCreation}} {{order.status}}</li>
    {{/each}}
</ul>
{{outlet}}